---
description: Fit, version, and run ML models on catalogued data — build an sklearn-style pipeline, fit it on a catalogued expression to get a FittedPipeline, then transform / predict / predict_proba and catalog it.
---

# ML

Stub — see `brief.txt` for intent.
