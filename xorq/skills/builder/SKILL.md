---
description: Work with xorq ExprBuilders — "an expr that builds exprs" whose tags carry domain metadata. Get to/from builder exprs and author custom TagHandlers. NOT the ML workflow (that is the ml skill).
---

# Builder

Stub — see `brief.txt` for intent.
